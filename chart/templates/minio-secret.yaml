# Copyright 2024-2026 Defense Unicorns
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Defense-Unicorns-Commercial

{{- if .Values.objectStorage.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: reference-package-minio-creds
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  {{- $accessKey := .Values.objectStorage.accessKey }}
  {{- $secretKey := .Values.objectStorage.secretKey }}
  {{- $secret := "" }}
  {{- if and .Values.objectStorage.existingSecret.name (eq .Values.objectStorage.accessKey "") }}
  {{- $secret = (lookup "v1" "Secret" .Release.Namespace .Values.objectStorage.existingSecret.name) }}
  {{- end }}
  {{- if $secret }}
  {{- $accessKey = index $secret.data .Values.objectStorage.existingSecret.accessKeyKey | b64dec }}
  {{- $secretKey = index $secret.data .Values.objectStorage.existingSecret.secretKeyKey | b64dec }}
  {{- end }}
  S3_ACCESS_KEY: {{ $accessKey | quote }}
  S3_SECRET_KEY: {{ $secretKey | quote }}
{{- end }}
